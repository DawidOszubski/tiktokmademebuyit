import 'package:flutter/material.dart';

class SingleShopItemWidget extends StatelessWidget {
  const SingleShopItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
